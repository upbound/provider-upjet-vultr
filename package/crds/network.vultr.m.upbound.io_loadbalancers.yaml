---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: loadbalancers.network.vultr.m.upbound.io
spec:
  group: network.vultr.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - vultr
    kind: LoadBalancer
    listKind: LoadBalancerList
    plural: loadbalancers
    singular: loadbalancer
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: LoadBalancer is the Schema for the LoadBalancers API. Get information
          about a Vultr Load Balancer.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LoadBalancerSpec defines the desired state of LoadBalancer
            properties:
              forProvider:
                properties:
                  attachedInstances:
                    description: Array of instances that are currently attached to
                      the load balancer.
                    items:
                      type: string
                    type: array
                  autoSslDomain:
                    description: The auto SSL domain configuration for a load balancer.
                      This can be a root domain (example.com) or include a subdomain
                      (sub.example.com).
                    type: string
                  balancingAlgorithm:
                    description: The balancing algorithm for your load balancer. Options
                      are roundrobin or leastconn. Default value is roundrobin
                    type: string
                  cookieName:
                    description: Name for your given sticky session.
                    type: string
                  firewallRules:
                    description: Defines the firewall rules for a load balancer.
                    items:
                      properties:
                        ipType:
                          description: The type of ip this rule is - may be either
                            v4 or v6.
                          type: string
                        port:
                          description: The assigned port (integer) on the attached
                            instances that the load balancer should check against.
                            Default value is 80.
                          format: int64
                          type: integer
                        source:
                          description: IP address with subnet that is allowed through
                            the firewall. You may also pass in cloudflare which will
                            allow only CloudFlares IP range.
                          type: string
                      type: object
                    type: array
                  forwardingRules:
                    description: List of forwarding rules for a load balancer. The
                      configuration of a forwarding_rules is listened below.
                    items:
                      properties:
                        backendPort:
                          description: Port on instance side.
                          format: int64
                          type: integer
                        backendProtocol:
                          description: 'Protocol on instance side. Possible values:
                            "http", "https", "tcp".'
                          type: string
                        frontendPort:
                          description: Port on load balancer side.
                          format: int64
                          type: integer
                        frontendProtocol:
                          description: 'Protocol on load balancer side. Possible values:
                            "http", "https", "tcp".'
                          type: string
                      type: object
                    type: array
                  globalRegions:
                    description: A set of region IDs to deploy child load balancers
                      to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  healthCheck:
                    description: A block that defines the way load balancers should
                      check for health. The configuration of a health_check is listed
                      below.
                    properties:
                      checkInterval:
                        description: Time in seconds to perform health check. Default
                          value is 15.
                        format: int64
                        type: integer
                      healthyThreshold:
                        description: Number of failed attempts encountered before
                          failover. Default value is 5.
                        format: int64
                        type: integer
                      path:
                        description: The path on the attached instances that the load
                          balancer should check against. Default value is /
                        type: string
                      port:
                        description: The assigned port (integer) on the attached instances
                          that the load balancer should check against. Default value
                          is 80.
                        format: int64
                        type: integer
                      protocol:
                        description: The protocol used to traffic requests to the
                          load balancer. Possible values are http, or tcp. Default
                          value is http.
                        type: string
                      responseTimeout:
                        description: Time in seconds to wait for a health check response.
                          Default value is 5.
                        format: int64
                        type: integer
                      unhealthyThreshold:
                        description: Number of failed attempts encountered before
                          failover. Default value is 5.
                        format: int64
                        type: integer
                    type: object
                  httpVersion:
                    description: Integer value that indicates if HTTP/2 or HTTP/3
                      is enabled. Allowed values 2 or 3.
                    format: int64
                    type: integer
                  label:
                    description: The load balancer's label.
                    type: string
                  nodes:
                    description: The number of nodes to add to the load balancer (1-99).
                      Must be an odd number. Default value is 1.
                    format: int64
                    type: integer
                  proxyProtocol:
                    description: Boolean value that indicates if Proxy Protocol is
                      enabled.
                    type: boolean
                  region:
                    description: The region your load balancer is deployed in.
                    type: string
                  ssl:
                    description: A block that supplies your ssl configuration to be
                      used with HTTPS. The configuration of a ssl is listed below.
                    properties:
                      certificate:
                        description: The SSL Certificate.
                        type: string
                      chain:
                        description: The SSL certificate chain.
                        type: string
                      privateKeySecretRef:
                        description: The SSL certificates private key.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  sslRedirect:
                    description: Boolean value that indicates if HTTP calls will be
                      redirected to HTTPS.
                    type: boolean
                  vpc:
                    description: A VPC ID that the load balancer should be attached
                      to.
                    type: string
                  vpcRef:
                    description: Reference to a VPC in network to populate vpc.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vpcSelector:
                    description: Selector for a VPC in network to populate vpc.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  attachedInstances:
                    description: Array of instances that are currently attached to
                      the load balancer.
                    items:
                      type: string
                    type: array
                  autoSslDomain:
                    description: The auto SSL domain configuration for a load balancer.
                      This can be a root domain (example.com) or include a subdomain
                      (sub.example.com).
                    type: string
                  balancingAlgorithm:
                    description: The balancing algorithm for your load balancer. Options
                      are roundrobin or leastconn. Default value is roundrobin
                    type: string
                  cookieName:
                    description: Name for your given sticky session.
                    type: string
                  firewallRules:
                    description: Defines the firewall rules for a load balancer.
                    items:
                      properties:
                        ipType:
                          description: The type of ip this rule is - may be either
                            v4 or v6.
                          type: string
                        port:
                          description: The assigned port (integer) on the attached
                            instances that the load balancer should check against.
                            Default value is 80.
                          format: int64
                          type: integer
                        source:
                          description: IP address with subnet that is allowed through
                            the firewall. You may also pass in cloudflare which will
                            allow only CloudFlares IP range.
                          type: string
                      type: object
                    type: array
                  forwardingRules:
                    description: List of forwarding rules for a load balancer. The
                      configuration of a forwarding_rules is listened below.
                    items:
                      properties:
                        backendPort:
                          description: Port on instance side.
                          format: int64
                          type: integer
                        backendProtocol:
                          description: 'Protocol on instance side. Possible values:
                            "http", "https", "tcp".'
                          type: string
                        frontendPort:
                          description: Port on load balancer side.
                          format: int64
                          type: integer
                        frontendProtocol:
                          description: 'Protocol on load balancer side. Possible values:
                            "http", "https", "tcp".'
                          type: string
                      type: object
                    type: array
                  globalRegions:
                    description: A set of region IDs to deploy child load balancers
                      to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  healthCheck:
                    description: A block that defines the way load balancers should
                      check for health. The configuration of a health_check is listed
                      below.
                    properties:
                      checkInterval:
                        description: Time in seconds to perform health check. Default
                          value is 15.
                        format: int64
                        type: integer
                      healthyThreshold:
                        description: Number of failed attempts encountered before
                          failover. Default value is 5.
                        format: int64
                        type: integer
                      path:
                        description: The path on the attached instances that the load
                          balancer should check against. Default value is /
                        type: string
                      port:
                        description: The assigned port (integer) on the attached instances
                          that the load balancer should check against. Default value
                          is 80.
                        format: int64
                        type: integer
                      protocol:
                        description: The protocol used to traffic requests to the
                          load balancer. Possible values are http, or tcp. Default
                          value is http.
                        type: string
                      responseTimeout:
                        description: Time in seconds to wait for a health check response.
                          Default value is 5.
                        format: int64
                        type: integer
                      unhealthyThreshold:
                        description: Number of failed attempts encountered before
                          failover. Default value is 5.
                        format: int64
                        type: integer
                    type: object
                  httpVersion:
                    description: Integer value that indicates if HTTP/2 or HTTP/3
                      is enabled. Allowed values 2 or 3.
                    format: int64
                    type: integer
                  label:
                    description: The load balancer's label.
                    type: string
                  nodes:
                    description: The number of nodes to add to the load balancer (1-99).
                      Must be an odd number. Default value is 1.
                    format: int64
                    type: integer
                  proxyProtocol:
                    description: Boolean value that indicates if Proxy Protocol is
                      enabled.
                    type: boolean
                  region:
                    description: The region your load balancer is deployed in.
                    type: string
                  ssl:
                    description: A block that supplies your ssl configuration to be
                      used with HTTPS. The configuration of a ssl is listed below.
                    properties:
                      certificate:
                        description: The SSL Certificate.
                        type: string
                      chain:
                        description: The SSL certificate chain.
                        type: string
                      privateKeySecretRef:
                        description: The SSL certificates private key.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    required:
                    - privateKeySecretRef
                    type: object
                  sslRedirect:
                    description: Boolean value that indicates if HTTP calls will be
                      redirected to HTTPS.
                    type: boolean
                  vpc:
                    description: A VPC ID that the load balancer should be attached
                      to.
                    type: string
                  vpcRef:
                    description: Reference to a VPC in network to populate vpc.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vpcSelector:
                    description: Selector for a VPC in network to populate vpc.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.forwardingRules is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.forwardingRules)
                || (has(self.initProvider) && has(self.initProvider.forwardingRules))'
            - message: spec.forProvider.region is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.region)
                || (has(self.initProvider) && has(self.initProvider.region))'
          status:
            description: LoadBalancerStatus defines the observed state of LoadBalancer.
            properties:
              atProvider:
                properties:
                  attachedInstances:
                    description: Array of instances that are currently attached to
                      the load balancer.
                    items:
                      type: string
                    type: array
                  autoSslDomain:
                    description: The auto SSL domain configuration for a load balancer.
                      This can be a root domain (example.com) or include a subdomain
                      (sub.example.com).
                    type: string
                  balancingAlgorithm:
                    description: The balancing algorithm for your load balancer. Options
                      are roundrobin or leastconn. Default value is roundrobin
                    type: string
                  cookieName:
                    description: Name for your given sticky session.
                    type: string
                  firewallRules:
                    description: Defines the firewall rules for a load balancer.
                    items:
                      properties:
                        id:
                          description: The load balancer ID.
                          type: string
                        ipType:
                          description: The type of ip this rule is - may be either
                            v4 or v6.
                          type: string
                        port:
                          description: The assigned port (integer) on the attached
                            instances that the load balancer should check against.
                            Default value is 80.
                          format: int64
                          type: integer
                        source:
                          description: IP address with subnet that is allowed through
                            the firewall. You may also pass in cloudflare which will
                            allow only CloudFlares IP range.
                          type: string
                      type: object
                    type: array
                  forwardingRules:
                    description: List of forwarding rules for a load balancer. The
                      configuration of a forwarding_rules is listened below.
                    items:
                      properties:
                        backendPort:
                          description: Port on instance side.
                          format: int64
                          type: integer
                        backendProtocol:
                          description: 'Protocol on instance side. Possible values:
                            "http", "https", "tcp".'
                          type: string
                        frontendPort:
                          description: Port on load balancer side.
                          format: int64
                          type: integer
                        frontendProtocol:
                          description: 'Protocol on load balancer side. Possible values:
                            "http", "https", "tcp".'
                          type: string
                        ruleId:
                          description: The load balancer ID.
                          type: string
                      type: object
                    type: array
                  globalRegions:
                    description: A set of region IDs to deploy child load balancers
                      to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  hasSsl:
                    description: Boolean value that indicates if SSL is enabled.
                    type: boolean
                  healthCheck:
                    description: A block that defines the way load balancers should
                      check for health. The configuration of a health_check is listed
                      below.
                    properties:
                      checkInterval:
                        description: Time in seconds to perform health check. Default
                          value is 15.
                        format: int64
                        type: integer
                      healthyThreshold:
                        description: Number of failed attempts encountered before
                          failover. Default value is 5.
                        format: int64
                        type: integer
                      path:
                        description: The path on the attached instances that the load
                          balancer should check against. Default value is /
                        type: string
                      port:
                        description: The assigned port (integer) on the attached instances
                          that the load balancer should check against. Default value
                          is 80.
                        format: int64
                        type: integer
                      protocol:
                        description: The protocol used to traffic requests to the
                          load balancer. Possible values are http, or tcp. Default
                          value is http.
                        type: string
                      responseTimeout:
                        description: Time in seconds to wait for a health check response.
                          Default value is 5.
                        format: int64
                        type: integer
                      unhealthyThreshold:
                        description: Number of failed attempts encountered before
                          failover. Default value is 5.
                        format: int64
                        type: integer
                    type: object
                  httpVersion:
                    description: Integer value that indicates if HTTP/2 or HTTP/3
                      is enabled. Allowed values 2 or 3.
                    format: int64
                    type: integer
                  id:
                    description: The load balancer ID.
                    type: string
                  ipv4:
                    description: IPv4 address for your load balancer.
                    type: string
                  ipv6:
                    description: IPv6 address for your load balancer.
                    type: string
                  label:
                    description: The load balancer's label.
                    type: string
                  nodes:
                    description: The number of nodes to add to the load balancer (1-99).
                      Must be an odd number. Default value is 1.
                    format: int64
                    type: integer
                  proxyProtocol:
                    description: Boolean value that indicates if Proxy Protocol is
                      enabled.
                    type: boolean
                  region:
                    description: The region your load balancer is deployed in.
                    type: string
                  ssl:
                    description: A block that supplies your ssl configuration to be
                      used with HTTPS. The configuration of a ssl is listed below.
                    properties:
                      certificate:
                        description: The SSL Certificate.
                        type: string
                      chain:
                        description: The SSL certificate chain.
                        type: string
                    type: object
                  sslRedirect:
                    description: Boolean value that indicates if HTTP calls will be
                      redirected to HTTPS.
                    type: boolean
                  status:
                    description: Current status for the load balancer
                    type: string
                  vpc:
                    description: A VPC ID that the load balancer should be attached
                      to.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
